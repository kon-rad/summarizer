{
    "title": "Text Summarization with Recursive Chunking",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "text": {
            "title": "Text to Summarize",
            "type": "string",
            "description": "The text content you want to summarize.",
            "editor": "textarea",
            "prefill": "Enter your text here..."
        },
        "modelName": {
            "title": "Model Name",
            "type": "string",
            "description": "The LLM model to use for summarization. Use OpenRouter format (e.g., openai/gpt-4o-mini, anthropic/claude-3.5-sonnet, meta-llama/llama-3.1-70b-instruct). See https://openrouter.ai/models for available models.",
            "editor": "textfield",
            "default": "openai/gpt-4o-mini",
            "prefill": "openai/gpt-4o-mini"
        },
        "summaryLength": {
            "title": "Summary Length",
            "type": "string",
            "description": "Desired length of the summary.",
            "editor": "select",
            "enum": ["short", "medium", "long"],
            "default": "medium"
        },
        "systemPrompt": {
            "title": "Custom System Prompt (Optional)",
            "type": "string",
            "description": "Optional custom system prompt to guide the summarization. If not provided, a default prompt will be used.",
            "editor": "textarea",
            "prefill": ""
        },
        "apiKey": {
            "title": "OpenRouter API Key",
            "type": "string",
            "description": "Your OpenRouter API key. Get one at https://openrouter.ai/keys. Can also use OPEN_ROUTER_API_KEY environment variable.",
            "editor": "textfield",
            "isSecret": true
        },
        "chunkSize": {
            "title": "Chunk Size (Optional)",
            "type": "integer",
            "description": "Maximum number of characters per chunk. Default is 4000.",
            "editor": "number",
            "default": 4000,
            "minimum": 1000,
            "maximum": 10000
        },
        "chunkOverlap": {
            "title": "Chunk Overlap (Optional)",
            "type": "integer",
            "description": "Number of characters to overlap between chunks. Default is 200.",
            "editor": "number",
            "default": 200,
            "minimum": 0,
            "maximum": 1000
        }
    },
    "required": ["text", "apiKey"]
}
